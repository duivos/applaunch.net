<?xml version="1.0" encoding="utf-8"?>
<Window
  x:Class="applaunch.WinUi.MainWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:local="using:applaunch.WinUi"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:models="using:applaunch.WinUi.Models"
  mc:Ignorable="d"
  Title="applaunch"
>
  <Window.SystemBackdrop>
    <MicaBackdrop />
  </Window.SystemBackdrop>

  <Grid Padding="14" Background="{StaticResource PrimaryBrush}">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TextBox
      x:Name="SearchBox"
      Grid.Row="0"
      PlaceholderText="Search apps..."
      FontSize="20"
      BorderThickness="0"
      TextChanged="SearchBox_TextChanged"
      KeyDown="SearchBox_KeyDown"
    />

    <ListView
      x:Name="ResultList"
      Grid.Row="1"
      Margin="0,10,0,0"
      ItemsSource="{x:Bind VisibleApps}"
    >
      <ListView.ItemTemplate>
        <DataTemplate x:DataType="models:AppItem">
          <TextBlock Text="{x:Bind Name}" FontSize="16" Padding="5,8" />
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </Grid>
</Window>
